# Third-party notices

## Doocs Markdown themes

The `doocs-classic`, `doocs-grace`, and `doocs-simple` built-in themes are
cleanly adapted from the theme CSS in [`doocs/md`](https://github.com/doocs/md)
at commit `fd136f79f84cf8f9c6206ef864fb318b16390171`.

The upstream project declares the WTFPL license. This repository adapts the CSS
to the plugin's `.wechat-article` HTML root and its safe rendering model; the
adaptation is distributed as part of the plugin under the repository's license.

The Doocs Vue application code, cloud modules, logos, icons, web assets, and
remote resources are not included in this plugin. No remote stylesheet or
runtime resource is loaded by these built-in themes.
