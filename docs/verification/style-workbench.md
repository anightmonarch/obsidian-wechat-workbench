# 样式工作台验证记录

## 自动化验证

验证日期：2026-08-21

- Obsidian 插件构建：`npm test` 通过。
- 样式工作台、渲染投影、发布哈希一致性专项测试通过。
- `npm run lint` 通过。
- `npm run scan:secrets` 通过。
- Doocs 三套黄金 HTML：`doocs-classic`、`doocs-grace`、`doocs-simple` 均有确定性回归文件。
- `npm run generate:code-themes` 二次生成结果字节一致。

## 本地 UI 验证边界

已用 jsdom 验证：

- 文章预览与发布设置页签切换；发布设置页不显示预览工具栏。
- 样式面板的主题、字体、字号、主题色、标题、代码、图注、段落控件。
- `Escape` 关闭样式面板；窄容器使用右侧浮层，不压缩正文预览。
- 样式切换的保存过程不向用户展示中间状态，不展示 hash、CSS 解析器或英文校验细节。

## 尚未执行的真实环境验证

以下项目需要在独立 Obsidian 测试 Vault 和已授权公众号账号中执行，不能由单元测试替代：

- 窄侧栏下的实际截图验收。
- 真实剪贴板粘贴到微信公众号编辑器后的视觉检查。
- 使用已授权账号创建或更新一个草稿，并在公众号后台核对结果。

## 已执行的真实 Obsidian 验证

在独立 Vault `/tmp/wechat-workbench-checkpoint-1`、Obsidian 1.13.7 中完成：

- 插件从 `.obsidian/plugins/wechat-workbench` 加载，文章预览和样式工作台可用。
- 重新打开独立 Vault 后，文章级 `wechat-style` 能读取并显示当前主题。
- 切换主题后 Frontmatter 更新，预览刷新，样式面板保持打开，可连续调整。
- 真实窗口截图确认中文主题、字体、字号和主题色控件可见；未显示内部校验细节。
- 样式面板只显示 `经典`、`优雅`、`简洁` 三个内置主题；默认“其他主题”分组及其四个旧主题不再出现。
- 样式按钮位于操作栏右侧且为紧凑按钮；样式面板以右侧覆盖层打开，不再为面板预留第二列，关闭面板时文章卡片占满 Workbench 内部可用宽度。
- 使用带本地 PNG 的文章完成真实图片验收：编辑器中的图片、预览中的图片和图片说明均可见；远程图片仍按本地预览安全策略不主动加载。
- 点击主题、字体和开关后，样式面板根节点保持不变，仅原地更新选中状态与预览结果；未再出现面板消失后重建的闪烁。
- 样式面板在样式保存触发预览刷新期间保持可见；标题下拉、代码主题和图片说明的标签与控件不再互相覆盖，选项文字可完整读取。
- 下拉控件明确使用 Obsidian 界面字体、固定行高和右侧安全内边距；实机可完整读取 `github-dark`、`仅 alt` 等中西文选项值。
- 样式面板头部设置为不可收缩的独立层，正文滚动区不会再绘制到头部下方；滚动到主题、字体和标题控件时文字保持完整可见。
- 样式面板按完整设置分区吸附滚动，停止滚动时不会停在按钮或输入控件的半行位置。
- 三套内置 Doocs 主题统一收紧 H2 上边距（`4em` 调整为 `2em`），实机预览中一级标题后的正文与二级标题之间不再出现过大的空白。
- 文章预览不再展示活动笔记连接摘要；带行号和 Mac 窗口样式的代码块只保留一份代码内容，代码行紧凑且顶部圆点位于代码背景内。
- 预览画布与样式面板分别滚动：预览滚动不带动样式面板，样式面板内部滚动时标题栏和关闭按钮保持固定。
- 普通样式控件统一使用中性背景，选中项统一使用绿色边框和文字；主题色按钮保留小色点作为必要的颜色识别。

本项目没有执行正式发布、群发、删除草稿、`git push`、npm 发布或社区提交。
